{
  "name": "stef",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development bankai start -c [ -u sheetify-sass -u sheetify-cssnext ] index.js -p 8080",
    "build": "npm run build-icons && npm run build-app && npm run build-staticfix",
    "build-app": "NODE_ENV=production bankai build -c [ -u sheetify-sass -u sheetify-cssnext ] index.js",
    "build-staticfix": "rm ./dist/sw.js || true && cp ./sw.js ./dist/sw.js && rm ./dist/favicon.ico || true && cp ./favicon.ico ./dist/favicon.ico && rm ./dist/CNAME || true && cp ./CNAME ./dist/CNAME && rm ./dist/200.html || true && cp ./dist/index.html ./dist/200.html",
    "build-icons": "svgo -f assets",
    "inspect": "bankai inspect index.js",
    "test": "standard",
    "publish": "git push && surge -p ./dist",
    "publish-test": "npm run build && surge -p ./dist -d stef-test.surge.sh"
  },
  "dependencies": {
    "babel-plugin-transform-es2015-arrow-functions": "6.22.0",
    "babelify": "7.3.0",
    "choo": "^6.0.0",
    "choo-devtools": "^1.3.1",
    "choo-log": "^7.2.1",
    "choo-service-worker": "^2.2.0",
    "sanitize.css": "^5.0.0",
    "sheetify-cssnext": "^1.0.7",
    "sheetify-sass": "^1.0.2"
  },
  "devDependencies": {
    "bankai": "^8.1.1",
    "standard": "^10.0.3",
    "svgo": "^0.7.2"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "babel": {
    "plugins": [
      [
        "transform-es2015-arrow-functions"
      ]
    ]
  }
}
